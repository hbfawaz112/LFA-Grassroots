import 'package:flutter/material.dart';
import 'package:scorefoot/ShowData/news_data.dart';


class news extends StatelessWidget {
  const news({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return  Container(
      child:news_data()
    
    );
     
  }
}

